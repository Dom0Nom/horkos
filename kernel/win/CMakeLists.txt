# kernel/win/CMakeLists.txt
# Windows KMDF driver build target. Requires WDK (Phase 3).
# This file is only included from the root CMakeLists on WIN32 hosts.
# No-op placeholder for Phase 1: WDK detection and the driver target land in Phase 3.

message(STATUS "kernel/win: WDK driver target deferred to Phase 3")
